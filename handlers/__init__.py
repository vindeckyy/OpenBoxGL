"""Handlers package for OpenBox capability modules.

``web_app.Handler`` inherits the mixin classes here. ``handlers/native.py`` is
wired by dotted route specs and takes the request handler as its first argument.
"""
